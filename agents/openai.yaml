interface:
  display_name: "Execution Plan"
  short_description: "Turn execution context into concrete steps"
  default_prompt: "Use $execution-plan to turn this execution context into a validated, executable plan artifact."
